# 시퀀스 객체의 요소 개수 구하기

# list 의 요소 개수 구하기
a = list(range(10))
print(len(a))

# tuple 의 요소 개수 구하기
b = tuple(range(5))
print(len(b))

# range 의 숫자 생성 개수 구하기
print(len(range(0, 10, 2)))

# 문자열의 길이 구하기
hello = "Hello, world!"
print(len(hello))